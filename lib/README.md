# lib

This directory contains the JAR dependencies used for running the experiments presented in the paper.
These are essentially the dependencies required by the WebGraph (https://webgraph.di.unimi.it) Java library.